apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-postgres.fullname" . }}
  labels:
    {{- include "common-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "common-postgres.selectorLabels" . | nindent 4 }}
